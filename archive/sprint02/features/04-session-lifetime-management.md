# Feature: Session Lifetime Management

## Capability

Give authenticated sessions a defined lifetime (expiry) while keeping them
stored server-side, so sign-out and revocation continue to work as today.