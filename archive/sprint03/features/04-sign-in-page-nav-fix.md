# Feature: Sign-In Page Navigation Fix

## Capability

Hide the entire top navigation bar (brand, menu links, and toggler) when a user
is not signed in; the navigation appears only once an authenticated session
exists.