# Feature: ORM-Backed Persistence

## Capability

Persist application data through a maintained ORM (SQLAlchemy), so application
code reads and writes via models rather than hand-managed SQL strings, providing
a maintainable persistence foundation.